fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
huggingface_hub>=0.24.0
soundfile>=0.12.1
numpy>=1.26.0
# Kokoro TTS (Apache 2.0 — downloads ~400MB ONNX model on first use)
kokoro-onnx>=0.4.0
# Stable Diffusion image generation via diffusers + PyTorch MPS (no HF auth required)
# Model: Lykon/dreamshaper-8 — downloads ~2GB on first use
diffusers>=0.30.0
accelerate>=0.30.0
safetensors>=0.4.0
